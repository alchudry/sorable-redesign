// import _ from 'lodash';
// import faker from 'faker';
// // Recommended Product Setup
// const dataRecommendedProduct = _.times(10, () => (
//   {
//     name: faker.commerce.productName(),
//     featuredImage: 'assets/images/product-' + faker.random.number({min: 1, max: 6}) + '.jpg',
//     price: faker.commerce.price(),
//     color: faker.commerce.color()
//   }
// ))
// const rows = [...Array( Math.ceil(dataRecommendedProduct.length / 2) )];
// export const productRows = rows.map( ( _, idx) => dataRecommendedProduct.slice(idx * 2, idx * 2 + 2) );

